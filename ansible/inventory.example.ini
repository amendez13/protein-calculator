[protein_calculator]
# Replace with your VPS IP/hostname.
vps.example.com ansible_user=ubuntu

[protein_calculator:vars]
# If you use a non-default SSH key:
# ansible_ssh_private_key_file=~/.ssh/id_ed25519

# Optional: enable nginx reverse proxy
protein_calculator_enable_nginx=false
protein_calculator_nginx_server_name=_
